# Copy to .env and fill in. .env is gitignored — never commits.

# === Active (free) stack ===

# Google Gemini — LLM + embeddings. Free, no card: aistudio.google.com
GEMINI_API_KEY=

# LLM fallback chain (order via LLM_CHAIN, default "qwen,groq,gemini").
# Providers without a key are skipped. Qwen via OpenRouter, Groq via Groq Cloud.
QWEN_API_KEY=
GROQ_API_KEY=

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=

# Access control. Only these email(s) become admin automatically; all other
# emails must be approved in Access Control before account creation.
APP_ADMIN_EMAILS=rk26.ftw@gmail.com
DEFAULT_ORGANIZATION_NAME=iORA Workspace

# Optional signup domain allowlist. Empty = any email domain may request access.
# Comma-separated domains restrict who can submit signup requests.
APP_ALLOWED_EMAIL_DOMAINS=

# Base URL the password-reset email link returns to. Must be added to Supabase
# Auth -> URL Configuration -> Redirect URLs. Use http://localhost:8000 locally.
APP_BASE_URL=https://docqa-chi.vercel.app

# Security headers: CSP is report-only by default so it can't break the SPA.
# Set true to enforce once violation reports look clean.
# CSP_ENFORCE=false

# Route reads through a user-JWT client so Postgres RLS also enforces (defense in
# depth). Off until verified live against the policies. RLS_SCOPED_READS=false

# === Provider switches (defaults are free Gemini) ===
# LLM_PROVIDER=gemini      # gemini | claude
# EMBED_PROVIDER=gemini    # gemini | voyage

# === Optional: only if you switch providers later ===
# ANTHROPIC_API_KEY=       # for LLM_PROVIDER=claude (paid)
# VOYAGE_API_KEY=          # for EMBED_PROVIDER=voyage
