# Copy to .env and fill in. .env is gitignored — never commits.

# Claude — answers + summaries
ANTHROPIC_API_KEY=

# Voyage AI — embeddings (RAG path)
VOYAGE_API_KEY=

# Supabase — auth + file storage + postgres + pgvector
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=

# Postgres direct connection (for pgvector queries)
# Settings -> Database -> Connection string -> URI (use the pooler URI)
SUPABASE_DB_URL=
