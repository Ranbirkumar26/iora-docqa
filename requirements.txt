# backend
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# file parsing
pandas
openpyxl

# llm + embeddings
anthropic
voyageai

# supabase (db + storage + auth + pgvector via RPC)
supabase

# frontend
streamlit
requests

# tests
pytest
