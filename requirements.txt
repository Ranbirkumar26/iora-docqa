# backend
fastapi
uvicorn[standard]
python-multipart
python-dotenv
slowapi               # rate limiting (login/signup/reset + ask/upload)

# file parsing
pandas
openpyxl
pypdf
python-docx

# structured-data (SQL) query path
duckdb

# llm + embeddings
google-genai          # gemini (default, free): LLM + embeddings
anthropic             # optional: claude provider
voyageai              # optional: voyage embeddings

# supabase (db + storage + auth + pgvector via RPC)
supabase

# frontend
streamlit
requests

# tests
pytest
