# backend
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# file parsing
pandas
openpyxl

# llm + embeddings
google-genai          # gemini (default, free): LLM + embeddings
anthropic             # optional: claude provider
voyageai              # optional: voyage embeddings

# supabase (db + storage + auth + pgvector via RPC)
supabase

# frontend
streamlit
requests

# tests
pytest
